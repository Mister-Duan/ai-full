[project]
name = "fast-api-demo"
version = "0.1.0"
description = "FastAPI 演示项目"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]==0.136.3"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["api"]