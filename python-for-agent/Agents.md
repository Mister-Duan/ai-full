## 工程说明

此工程包含`Python`入门课程的所有课件、示例、练习题及答案。

每一个子文件夹表示一节课。

该课程面向`Python`零基础但具有其他语言（如`JavaScript`或`Java`）的开发经验，因此在授课节奏上会更明快，因为语言之间很多概念是想通的。

由于这个背景的存在，我们在编写`Python`课件的时候，不会详细介绍通用的概念（比如什么是变量、函数、类、对象等等），而是重点罗列`Python`语言自身的特性，比如：

- 在`python`中如何定义函数
- 在`python`中如何创建类的实例
- `python`中的独有特性
- ...

课件中，无须去对比其他语言，因为你不知道学员之前接触的是什么语言，仅需关注`python`自身即可。

## 编程规范

- 所有的代码必须带类型标注

## 沟通规范

**非常重要：当用户提问时，不能修改任何东西，不能新增任何东西，不能删除任何东西，仅回答用户问题即可**
