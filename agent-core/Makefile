.PHONY: dev build preview install

# 启动 HTML 工程开发服务器
dev:
	cd HTML && pnpm install && pnpm dev

# 构建生产版本
build:
	cd HTML && pnpm install && pnpm build

# 预览生产构建
preview:
	cd HTML && pnpm preview

# 安装依赖
install:
	cd HTML && pnpm install
